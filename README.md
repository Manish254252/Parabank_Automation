# Parabank Automation (Playwright + Cucumber + Java)

## How to Run

1. Clone the repo
2. Install dependencies: `mvn clean install`
3. Run tests: `mvn test`

This will open Playwright browser, perform signup, login, and print the account balance.
